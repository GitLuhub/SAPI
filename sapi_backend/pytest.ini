[pytest]
asyncio_mode = auto
addopts = --cov=app --cov-report=term-missing --cov-fail-under=80
markers =
    integration: Tests that require a real PostgreSQL database (skipped by default, run with --integration)
